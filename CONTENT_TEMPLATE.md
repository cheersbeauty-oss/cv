# 作品集内容维护模板

页面内容集中在 [index.html](index.html) 底部的 `portfolioData` 对象中。修改数据后，页面会自动重新生成时间线、作品卡片和技能标签，不需要改 HTML 结构。

## 模块总览

| 模块 | 用途 | 对应页面区域 |
| --- | --- | --- |
| `profile` | 姓名、所在地、联系方式等基础资料 | 页眉与个人信息 |
| `experience` | 按时间排序的履历经历 | 沿着时间线走走 |
| `works` | 作品案例与作品详情入口 | 路上捡到的作品 |
| `skills` | 技能标签 | 小猫带在身上的东西 |

## 1. 个人资料

```js
profile: {
  name: '田荣',
  location: '张家界 / 香港',
  email: 'your@email.com'
}
```

字段说明：

- `name`：姓名。
- `location`：首屏路径或所在地文案。
- `email`：联系邮箱，当前联系按钮使用该地址。

## 2. 新增一段经历

把下面对象追加到 `experience` 数组中。建议按照时间从早到晚排列：

```js
{
  date: '2025.01 — 现在',
  title: '遇见新的风景',
  place: '机构名称 · 职位名称',
  text: '用一句到两句说明负责的工作，以及这段经历带来的能力。',
  frame: 'FRAME 092 / NEW VIEW',
  art: ['✦', '☀', '⌁'],
  theme: 'f-office',
  tags: ['能力一', '能力二', '能力三']
}
```

字段说明：

- `date`：时间段，建议统一使用 `YYYY.MM — YYYY.MM`。
- `title`：时间线上的叙事标题。
- `place`：机构与职位，会显示在正文第一行。
- `text`：经历描述，会显示在正文第二行。
- `frame`：画面编号和英文短标题。
- `art`：画面中的 3 个符号，依次对应左下、右上和顶部。
- `theme`：画面配色，目前可用 `f-office`、`f-account`、`f-tax`、`f-write`。
- `tags`：最多建议放 3 个能力标签。

## 3. 新增一个作品

把下面对象追加到 `works` 数组中：

```js
{
  type: 'CASE 04 / NEW PROJECT',
  title: '作品标题',
  text: '作品解决了什么问题，使用了什么方法，最后留下了什么结果。',
  art: ['◌', '✦'],
  link: '查看作品 ↗'
}
```

字段说明：

- `type`：作品编号和分类，建议使用英文大写。
- `title`：作品名称。
- `text`：作品简介，控制在 1 到 3 句。
- `art`：作品卡上的两个视觉符号。
- `link`：作品卡底部链接文字。首页会自动链接到 `works.html#case-编号`。

> 新增作品后，如果要在作品详情页显示完整内容，需要在 [works.html](works.html) 里增加对应的 `case-4` 详情块，并把首页链接目标改为对应详情地址。

## 4. 新增技能

把字符串追加到 `skills` 数组中：

```js
skills: [
  '把复杂的事拆开',
  '新的技能名称'
]
```

## 更新流程

1. 编辑 [index.html](index.html) 中的 `portfolioData`。
2. 运行 `npm run dev` 查看页面。
3. 运行 `npm run build` 检查构建。
4. 确认无误后执行 `git add . && git commit -m "更新作品集内容" && git push`。

## 内容维护建议

- 经历按时间顺序排列，最新经历放在最后。
- 一条经历只表达一个阶段，避免把多个职位合并成一段。
- `text` 以事实和动作开头，避免只写形容词。
- 每个作品尽量补充真实图片、视频或外部链接，再替换占位符号。
- 邮箱、手机号等隐私信息发布前确认是否适合公开。
